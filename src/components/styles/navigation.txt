


 /* Base styles */
.gold-top-menu {
  background-color: #d4af37; /* Or your preferred gold color */
  width: 100%;
  position: relative;
}

.topnav {
  display: flex;
  flex-direction: column;
  padding: 10px;
  overflow: hidden;
}

.nav-links-container {
  display: flex;
  flex-direction: column;
}

/* Navigation links */
.nav-link, .dropbtn {
  display: inline-block;
  color: #000;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  font-size: 16px;
  border: none;
  background: none;
  cursor: pointer;
  width: 100%;
  text-align: left;
}

.nav-link:hover, .dropbtn:hover {
  background-color: #c0a030;
}

/* Dropdown container */
.dropdown {
  position: relative;
  display: block;
  width: 100%;
}

/* Dropdown content (hidden by default) */
.dropdown-content {
  display: none;
  position: static;
  background-color: #e6d9a5;
  min-width: 100%;
  box-shadow: 0px 8px 16px 0px rgba(0,0,0,0.2);
  z-index: 1;
}

/* Links inside the dropdown */
.dropdown-content a {
  color: black;
  padding: 12px 16px;
  text-decoration: none;
  display: block;
  text-align: left;
}

/* Show the dropdown menu when needed */
.dropdown-content.show {
  display: block;
}

/* Mobile menu button */
.mobile-menu-btn {
  display: block;
  position: absolute;
  right: 10px;
  top: 10px;
  background: none;
  border: none;
  color: #000;
  font-size: 20px;
  cursor: pointer;
}

/* Desktop styles */
@media screen and (min-width: 768px) {
  .topnav {
    flex-direction: row;
    align-items: center;
  }
  
  .nav-links-container {
    flex-direction: row;
    flex-wrap: wrap;
  }
  
  .nav-link, .dropbtn {
    width: auto;
    text-align: center;
  }
  
  .dropdown {
    width: auto;
  }
  
  .dropdown-content {
    position: absolute;
    min-width: 160px;
  }
  
  .mobile-menu-btn {
    display: none;
  }
  
  /* When menu is open in desktop, we don't want the responsive class to affect layout */
  .topnav.responsive {
    flex-direction: row;
  }
  
  .topnav.responsive .nav-links-container {
    flex-direction: row;
  }
}

/* Hide regular menu items on mobile when menu is closed */
@media screen and (max-width: 767px) {
  .nav-links-container {
    display: none;
  }
  
  .topnav.responsive .nav-links-container {
    display: flex;
    margin-top: 40px;
  }
  
  .topnav.responsive .dropdown {
    float: none;
  }
  
  .topnav.responsive .dropdown-content {
    position: static;
  }
  
  .topnav.responsive .nav-link,
  .topnav.responsive .dropdown .dropbtn {
    text-align: left;
    width: 100%;
  }
}